# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories
node_modules/
jspm_packages/

# Next.js build output
.next/
out/

# Firebase configuration & secrets
# CRITICAL: Ignore service account credentials key and local user config
scanner/serviceAccountKey.json
scanner/config.json
.firebaserc
# (We can keep firebase.json as it defines deployment targets, but .firebaserc is optional/can be ignored or kept depending on preference. Keeping firebase.json is normal).

# Environments & secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Operating System Files
.DS_Store
Thumbs.db
